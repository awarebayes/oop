//
// Created by dev on 5/3/22.
//

#include <manager/inc/transform_manager.h>
#include "math/inc/transformation.h"

TransformManager &TransformManager::rotate(float x, float y, float z)
{
	//transformable.transform = CompositeTransformation(transformable.transform)
	//						 .compose(Rotation(x, y, z).share());
}

